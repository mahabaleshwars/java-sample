name: New PR Test

on: 
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        os: [micrsoft,zulu,oracle]
        version: [17,21]
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Java
        uses: actions/setup-java@730
        with:
          distribution: ${{ matrix.os }}
          java-version: ${{ matrix.version }}
