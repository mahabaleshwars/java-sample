name: Publish Maven package to GPR

on:
 
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
        java-version: [17]

    steps:
    - uses: actions/checkout@v4

    - name: Verify settings-security.xml exists
      run: |
       if (-not (Test-Path "$HOME/.m2/settings-security.xml")) {
          echo "settings-security.xml does not exist."
          exit 1
       }

    - name: Encrypt Maven passphrase
      id: encrypt-passphrase
      run: |
        ENCRYPTED_PASSPHRASE=$(mvn --encrypt-password "${{ secrets.MY_GPG_PASSPHRASE }}")
        echo "::set-output name=encrypted-passphrase::$ENCRYPTED_PASSPHRASE"

    
    
    - name: Set up Apache Maven Central
      uses: gowridurgad/setup-java-fork@testing-snap
      with: # running setup-java again overwrites the settings.xml
        distribution: 'sapmachine'
        java-version: ${{ matrix.java-version }}
        server-id: central # Value of the distributionManagement/repository/id field of the pom.xml
        server-username: MAVEN_USERNAME # env variable for username in deploy
        server-password: MAVEN_CENTRAL_TOKEN # env variable for token in deploy
        gpg-private-key: ${{ secrets.GPG_PRIVATE_KEY }} # Value of the GPG private key to import
        gpg-passphrase: ${{ secrets.MY_GPG_PASSPHRASE }} # env variable for GPG private key passphrase

   

    - name: Publish to Apache Maven Central
      run: mvn -P release --batch-mode deploy -DskipTests
      env:
        MAVEN_USERNAME: +ZArALIw
        MAVEN_CENTRAL_TOKEN: HiFxjNaL7H9uvVzZ5W+w8ssoG5dAaJgw8D4Av1WzdvGg
        MY_GPG_PASSPHRASE: ${{ secrets.MY_GPG_PASSPHRASE }}
